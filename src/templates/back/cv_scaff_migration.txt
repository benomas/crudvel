<?php
use Illuminate\Support\Facades\Schema;
use Illuminate\Database\Schema\Blueprint;

class Create<##[plural|studly](resource)##>Table extends \Customs\Crudvel\Database\Migrations\BaseMigration{
  /**
   * Run the migrations.
   */
  public function up(){
    if(!Schema::hasTable($this->mainTable)){
      Schema::create($this->mainTable, function (Blueprint $table) {
        Schema::disableForeignKeyConstraints();
        $table->bigIncrements('id');
        $table->string('name');
        $table->text('description')->nullable();
        $this->defaultColumns($table);
        Schema::enableForeignKeyConstraints();
      });
    }
  }
}
