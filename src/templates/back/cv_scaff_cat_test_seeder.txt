<?php namespace Database\Seeds\Test;

use Illuminate\Support\Facades\Schema;
use Illuminate\Database\Eloquent\Factories\Sequence;
use App\Models\{<##[singular|studly](resource)##>};

class <##[singular|studly](resource)##>TableSeeder extends \Customs\Crudvel\Database\Seeds\BaseSeeder{
  protected $seedsToInsert = 10;

	public function run(){
    Schema::disableForeignKeyConstraints();
    \Illuminate\Support\Facades\DB::transaction(function() {
      $<##[plural|camel](resource)##> = <##[singular|studly](resource)##>::factory()->count($this->getSeedsToInsert())->create();

      if($this->getInjectedSeedControl())
        $this->getInjectedSeedControl()->afterCreation($<##[plural|camel](resource)##>,$this);
    });
    Schema::enableForeignKeyConstraints();
	}
}
