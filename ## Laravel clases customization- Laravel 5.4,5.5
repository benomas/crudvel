## Laravel clases customization- Laravel 5.0

Default initializacion of laravel clases reduce code when making cruds

## Instalación

Add dependency on our composer.json at the root of laravel.

```js
{
    "require": {
        "benomas/crudvel": "dev-master"
    }
}
```

```bash
    composer update
```